import { product }      from "./product"
import { category }     from "./category"
import { blogPost }     from "./blogPost"
import { siteSettings } from "./siteSettings"

export const schemaTypes = [product, category, blogPost, siteSettings]
